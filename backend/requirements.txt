# 术力口周榜后端依赖
# 安装：pip install -r requirements.txt
# 注：scrapling 为 local editable 安装，需先 pip install -e 路径/Scrapling-0.4.12

anyio==4.14.1
fastapi==0.139.0
httpx==0.28.1
lxml==6.1.1
numpy==2.5.0
pandas==3.0.3
pydantic==2.13.4
requests==2.34.2
starlette==1.3.1
uvicorn==0.51.0
pycryptodome==3.23.0
# 网易云 weapi 加密由 backend/app/vendor/weencrypt.py 自实现（MIT），仅依赖 pycryptodome（上方已锁定）
# scrapling 本地路径：pip install -e ../Scrapling-0.4.12
