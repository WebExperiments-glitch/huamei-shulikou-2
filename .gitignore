# ===== 依赖 =====
node_modules/
__pycache__/
*.pyc
*.egg-info/
.venv/
venv/
# npm 锁文件（项目用 npm；pnpm-lock 为混用残留，不提交）
pnpm-lock.yaml

# ===== 构建产物 =====
dist/
frontend/dist/
build/

# ===== 密钥 / 环境变量（切勿提交，含 AI / Tavily 等真实 key）=====
.env
.env.*
!.env.example
.mcp.json

# ===== 本地数据库（含官方数据源，体积大且含本地路径，不进版本库）=====
data/
*.sqlite
*.sqlite3
*.db
biliboard-database/

# ===== 临时测试脚本（开发用，不进版本库）=====
backend/.agent_*.py
backend/probe_*.py
backend/.webfetch_test/

# ===== 本地工作区 / 记忆（不对外）=====
.workbuddy/
.jez/

# ===== 编辑器 / 系统 =====
.idea/
.vscode/
*.swp
.DS_Store
*.log

# ===== AI 模型与本地推理运行时（体积巨大，不进版本库；使用者需自备）=====
# GGUF 权重（4B/2B 等，单文件 1~3GB）
*.gguf
# 本地推理目录：含模型、llama.cpp Windows 二进制、探针与日志
ai agents/
llama-b10332-bin-win-vulkan-x64/
# 模型探针 / 临时二进制
.full_probe.bin
.head_chunk.bin
.umi.js
.llama*.log
.ms_search.html

# ===== 运行时日志（顶层）=====
backend_runtime.log

# ===== 第三方库 vendoring（BSD-3-Clause 宽松，允许随仓库发布；若需更轻量可删此目录并改 requirements.txt 指向 PyPI）=====
# Scrapling-0.4.12/

# ===== 大型第三方框架源码（只读参考/下载包，非项目运行时依赖，不进版本库）=====
# 这些是硬盘上的第三方源码，体积巨大且与项目源码无运行时引用关系。
# 说明：dsh(1.5GB koishi)、airi_src(530MB)、airi_latest(575MB)、airi(2MB)、
#       sonic-topography(488MB)、deepseek_v3_tokenizer(7.7MB)。
#       注意：规则行不写行内注释——git 对含中文的行内注释解析可能失效，说明统一放这里。
dsh/
airi/
airi_src/
airi_latest/
sonic-topography/
sonic-topography-master.zip
deepseek_v3_tokenizer/
airi-main.zip
download_airi.ps1

# ===== 打包/下载遗留 =====
*.zip
start_all.bat

# ===== Electron 桌面版构建产物（体积巨大，不入库；从 cloud 分发随 Release 下载）=====
app/desktop/node_modules/
app/desktop/build_out/
app/desktop/release/
app/desktop/build/

# ===== 后端打包产物 / 内置浏览器（体积巨大，不入库；随桌面 Release 分发）=====
backend/dist_backend/
backend/build_backend/
backend/.playwright-browsers/

# ===== iOS 崩溃诊断（Apple Trace 归档可达数 GB，不入库；仅本机排查用）=====
ios/BAG/

# ===== 已弃用：netease_encode_api（AGPLv3，与本项目 CC BY-NC 4.0 不兼容）=====
# 本地备份保留在磁盘，但不进入开源版本库；其搜索/详情/评论功能已由
# 项目自带的 backend/app/vendor/weencrypt.py（MIT，自实现 WeAPI 加密）替代。
backend/app/vendor/netease_encode_api/
